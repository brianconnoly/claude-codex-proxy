---
description: Check whether the local Anthropic-compatible proxy is running and reachable.
---

Call the `anthropic_proxy_health` MCP tool. Summarize the result in one or two sentences.

If the proxy is not reachable, tell the user to start it from the project directory with `npm start`.
