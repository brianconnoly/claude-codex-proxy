---
description: List the Claude-compatible model aliases exposed by the local Anthropic proxy.
---

Call the `anthropic_proxy_models` MCP tool. Report the advertised model ids and their input/output token limits.

If the tool fails, mention that the local proxy should be running at `http://127.0.0.1:8787`.
